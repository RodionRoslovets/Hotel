include ./radioEl/radioEl.pug
mixin radioBtns(arr, name)
		.radio-wrapper
			.radio__list
				each el in arr
					.radio__item
						+radioEl(el.param, name)
						.radio__item-description
							p #{el.val}	